module Function;
# <- keyword
#      ^ variable

function f(param1: uint8, param2: vector<uint8>): uint64 {
# <- keyword
#        ^ function
#          ^ variable
#                  ^ type
    local xs: vector<uint64>;
    # <- keyword
    #         ^ type
    #                ^ type
    for (x in xs) {
    # <- keyword
    #      ^ keyword
        continue;
        # <- keyword
    }

    while (True) {
    # <- keyword
    #      ^ number
        stop;
        # <- keyword
    }
    return 500 + param1 + ! param2;
    # <- keyword
    #      ^ number
    #          ^
    #            ^ variable
    #                   ^
    #                     ^
    #                       ^ variable
}

global x = f(1, 2);
#          ^ function
